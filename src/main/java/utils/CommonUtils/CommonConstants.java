package utils.CommonUtils;
/**
 * @author ssamaji.
 * Created Feb 10, 2024.
 */
public class CommonConstants {

    public enum CONFIGS {
        DYNAMODB_USERNAME,
        POLICY_NAME,
        DB_CONFIG,
        SERVICE_CONFIG,
        DB_URL,
        PORT,
        HTTP_TYPE,
        URL,
        DB_PASSWORD,
        DB_USERNAME,
    }
}
